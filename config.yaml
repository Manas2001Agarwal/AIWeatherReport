vector_db:
  index_name: "ai-weather-chatbot"

retriever:
  top_k: 3
  score_threshold: 0.5

embedding_model:
  provider: "google"
  model_name: "models/text-embedding-004"

llm:
  google:
    provider: "google"
    model_name: "gemini-1.5-pro"
  groq:
    provider: "groq"
    model_name: "openai/gpt-oss-120b"